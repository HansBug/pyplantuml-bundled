<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTD/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Apple Silicon enforces W^X. The OpenJDK JVM mmap()s its codecache
         with PROT_EXEC at startup regardless of -Xint, so it needs JIT
         allowance. com.apple.security.cs.allow-jit is honoured for
         ad-hoc-signed binaries when no hardened-runtime profile is in
         force. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.disable-executable-page-protection</key>
    <true/>
</dict>
</plist>
